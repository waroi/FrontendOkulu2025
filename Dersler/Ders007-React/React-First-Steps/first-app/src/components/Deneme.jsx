const Deneme = ({ isim, yas }) => {
  return (
    <div style={{ backgroundColor: "blue", padding: "2rem" }}>
      <div>İsim: {isim}</div>
      <div>Yaş: {yas}</div>
    </div>
  );
};

export default Deneme;
